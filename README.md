# FIRMS-dashboard
Use NASA FIRMS (Fire Information for Resource Management System)  satellite data to pull active fire detections Simple web dashboard fetches FIRMS CSV, plots detections on a map, and sends alerts when new hotspots appear in a watch zone.
